---
title: Get Started
description: Set up tracing for your AI agent in minutes
---

## 1. Get API Keys

Create API Keys either with [TraceRoot Cloud](https://app.traceroot.ai) or [Self-Hosting](/developer/self-hosting).

## 2. Install the SDK

<CodeGroup>
```bash pip
pip install traceroot
```

```bash npm
npm install traceroot-sdk-ts
```

```bash uv
uv add traceroot
```

```bash poetry
poetry add traceroot
```
</CodeGroup>

## 3. Set Environment Variables

```bash .env
TRACEROOT_API_KEY=your-api-key
TRACEROOT_HOST_URL=https://app.traceroot.ai  # or your self-hosted URL
```

## 4. Instrument Your Code

Add a single line at the start of your application to instrument all OpenAI API calls.

```python
import traceroot
from traceroot import Integration
from openai import OpenAI

# This single line instruments all OpenAI API calls
traceroot.initialize(integrations=[Integration.OPENAI])

client = OpenAI()
```

For full SDK details, see the [Python SDK](/tracing/python-sdk) or the [TypeScript SDK](/tracing/typescript-sdk).

## 5. View Your Traces

Open [TraceRoot Cloud](https://app.traceroot.ai) (or your self-hosted dashboard), navigate to your project, and you'll see the full span hierarchy, token usage, cost, and timing.

---

## What's Next?

<CardGroup cols={2}>
  <Card title="Python SDK" icon="code" href="/tracing/python-sdk">
    Full API reference for decorators, spans, and configuration.
  </Card>
  <Card title="TypeScript SDK" icon="code" href="/tracing/typescript-sdk">
    Node.js SDK for wrappers, context propagation, and OpenInference integrations.
  </Card>
  <Card title="AI Agent" icon="brain" href="/ai-agent/overview">
    Auto-debug failing traces with the built-in AI agent.
  </Card>
</CardGroup>
